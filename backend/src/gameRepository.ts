import { Data } from '@checkers/generic'

export const createGameRepository = () => { 


  const addUserIfNeeded = (): Data => {
    return { } as Data
  }

  const addMove = () => {
    
  }


  return { 

  }

}